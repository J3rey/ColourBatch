{
  "name": "colourbatch",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n frontend,backend -c cyan,magenta \"npm --workspace frontend run dev\" \"npm --workspace backend run dev\"",
    "build": "npm --workspace frontend run build",
    "check": "npm --workspace frontend run build && npm --workspace backend run check",
    "preview": "npm --workspace frontend run preview",
    "start": "npm --workspace backend run start"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
